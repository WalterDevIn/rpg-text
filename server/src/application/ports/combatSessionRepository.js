/**
 * Combat session repository contract.
 * Implementations provide allocateId, save, findById, has, and remove.
 */
export const combatSessionRepositoryPort = Object.freeze([
  "allocateId", "save", "findById", "has", "remove",
]);
